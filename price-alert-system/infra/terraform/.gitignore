# Terraform state — contains plaintext secrets, never commit
terraform.tfstate
terraform.tfstate.backup

# Provider plugins and lock file cache — regenerated by terraform init
.terraform/

# Commit .terraform.lock.hcl for reproducible provider versions
# Uncomment the line below ONLY if you intentionally want to ignore it:
# .terraform.lock.hcl

# Secrets — NEVER commit
secrets.auto.tfvars

# Saved plans
*.tfplan
